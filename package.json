{
  "name": "team-task-manager-root",
  "version": "1.0.0",
  "description": "Root package.json for Railway deployment",
  "scripts": {
    "postinstall": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "node backend/seed.js && node backend/app.js"
  },
  "engines": {
    "node": ">=20.19.0"
  }
}
